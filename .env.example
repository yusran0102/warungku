# Database (same as your Next.js DATABASE_URL)
DATABASE_URL="postgresql://user:password@localhost:5432/warung_ku"

# JWT secret (replaces NEXTAUTH_SECRET)
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# App
PORT=3000
APP_ENV=development   # set to "production" in prod
